# -*- mode: python -*-

Import('env')

env = env.Clone()

env.SConscript(dirs=['commands', 'exec', 'management'], exports=['env'])

env.Alias('+streams_all_unittests', [
    '+streams_document_source_feeder_test',
    '+streams_document_source_wrapper_operator_test',
    '+streams_in_memory_source_sink_operator_test',
    '+streams_parser_test',
    '+streams_manager_test',
    '+streams_delayed_watermark_generator_test',
    '+streams_watermark_combiner_test',
    '+streams_document_event_time_extractor_test',
])
