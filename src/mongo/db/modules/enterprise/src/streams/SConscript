# -*- mode: python -*-

Import('env')

env = env.Clone()

env.SConscript(dirs=['commands', 'exec', 'management'], exports=['env'])

env.Alias('+streams_all_unittests', [
    '+document_source_feeder_test',
    '+document_source_wrapper_operator_test',
    '+in_memory_source_sink_operator_test',
    '+parser_test',
    '+delayed_watermark_generator_test',
    '+watermark_combiner_test',
    '+document_event_time_extractor_test',
])
